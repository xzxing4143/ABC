 import {Timer} from 'Timer.js';
 /**
  * 这是一个用于网页的多个定时调度的控制器 设计功能如下：
  * 可以同时注册多个定时任务，每个定时任务可以任意的删除，暂停或恢复暂停等功能
  */
 const TimerHelper=class {
     constructor(){
        
     }
     
     //添加定时管理时间，返回值是该时间管理对象的一个引用
     submit(fn,delay){

     }
     

     
 }